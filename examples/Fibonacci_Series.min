// Program to Display Fibonacci Sequence

program

//Variable declarations
declare i, n, t1 := 0,  t2 := 0, nextTerm

print "Enter the number of terms:"
declare in
in := input()
print "Fibonacci Series: "

for (i := 1, i<= in, ++i) {
	
	print(t1)
	nextTerm := t1 + t2
	t1 := t2
	t2 := nextTerm
	
	}
	
exit

/*
Program exited /* test */
*/

